{
	"name": "@study-reader/study-format",
	"version": "0.1.0",
	"private": true,
	"type": "module",
	"description": ".study course format: spec, JSON Schemas and a TypeScript toolkit",
	"exports": {
		".": "./src/index.ts"
	},
	"scripts": {
		"test": "vitest run",
		"check": "tsc --noEmit"
	},
	"dependencies": {
		"fflate": "^0.8.2",
		"zod": "^3.24.0"
	},
	"devDependencies": {
		"@types/node": "^22.10.0",
		"typescript": "^5.7.0",
		"vitest": "^3.0.0"
	}
}
