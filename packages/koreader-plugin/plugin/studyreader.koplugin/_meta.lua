local _ = require("gettext")

return {
    fullname = _("StudyReader"),
    description = _([[Study .study courses: read lessons, answer quizzes and review flashcards with spaced repetition — fully offline.]]),
}
